const ProblemSection = () => (
  <section className="mx-auto max-w-6xl px-4 py-14 md:px-6">
    <div className="grid gap-6 md:grid-cols-[0.9fr_1.1fr]">
      <div>
        <div className="eyebrow mb-3">Why it exists</div>
        <h2 className="section-title">Random teammates make ranked harder than it needs to be.</h2>
      </div>
      <p className="text-base leading-7 text-clutch-muted">
        Wrong roles, bad time overlap, mismatched region, or no communication can ruin a session. ClutchQ keeps those signals visible before anyone commits to a lobby.
      </p>
    </div>
  </section>
);

export default ProblemSection;
