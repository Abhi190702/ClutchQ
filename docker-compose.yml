services:
  mongo:
    image: mongo:7
    container_name: clutchq-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - clutchq-mongo-data:/data/db

volumes:
  clutchq-mongo-data:
